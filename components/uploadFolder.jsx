import { useRouter } from 'next/router';  
export default function UploadFolder() {
    // a modal that asks for a folder's name and has multiple file input
    // essentially creates the folder and uploads the files inside it and then refreshes to show it
    return (
        <div className="max-w-md">
        </div>
      )
};